source "https://rubygems.org"

# Use GitHub Pages to manage all dependencies
gem "github-pages", group: :jekyll_plugins

# Only add plugins here if you need a version newer than GitHub Pages provides
# group :jekyll_plugins do
#   gem "jekyll-menus"
# end

platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

# gem "wdm", "~> 0.2.0", :platforms => [:mingw, :x64_mingw, :mswin] # Remove or comment out if using Ruby 3.4+
# gem "webrick" # Remove, github-pages manages this

# gem "http_parser.rb", "~> 0.6.0", :platforms => [:jruby]
